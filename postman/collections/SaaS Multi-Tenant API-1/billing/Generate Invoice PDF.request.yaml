$kind: http-request
url: "{{base_url}}/tenants/{{tenant_id}}/billing/invoices/{{invoice_id}}/generate-pdf"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 2000
