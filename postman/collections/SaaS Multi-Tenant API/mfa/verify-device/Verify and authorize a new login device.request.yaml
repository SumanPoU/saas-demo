$kind: http-request
url: "{{baseUrl}}/mfa/verify-device"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "token": "<string>"
    }
examples: ./.resources/Verify and authorize a new login device.resources/examples
order: 1000
