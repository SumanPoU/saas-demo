$kind: http-request
name: "Step 1: Initiate MFA setup, generates a TOTP secret and QR code"
url: "{{baseUrl}}/mfa/setup"
method: POST
body:
  type: text
  content: ""
examples: ./.resources/Step 1- Initiate MFA setup, generates a TOTP secret and QR code.resources/examples
order: 1000
